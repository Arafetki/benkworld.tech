import type { NavItem } from "@/types";

export const navItems: Readonly<NavItem[]> = [
    {
        name: "About",
        href: "/about"
    },
    {
        name: "Blog",
        href: "/blog"
    }
];