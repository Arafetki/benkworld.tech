import { Metadata } from "next";

export const metadata: Metadata = {
    title: "About me"
}

export default function About() {
    return (
        <div>
            <h1>About page</h1>
        </div>
    );
}